# 2026-02-22

## Session Summary (compaction flush)

### MCP Server Deployed & Working
- Full MCP server live at `https://fiberagent.shop/api/mcp` (Streamable HTTP, stateless)
- 5 tools, 3 resources, 2 prompts — all tests pass
- Dynamic imports workaround for Vercel ESM bundling
- Claude Desktop config: `{"mcpServers":{"fiberagent":{"url":"https://fiberagent.shop/api/mcp"}}}`

### Oracle Agent Feedback Addressed
- P0s fixed: expanded catalog (16 real products), stats endpoint, agent lookup
- P1s fixed: fiber-proxy removed from agent-card, NL task endpoint added
- P2s remaining: Google A2A protocol, bidirectional callbacks

### Demo Comparison Page
- Static HTML at `/demo-comparison.html`
- Price research: Nike.com vs Zappos vs Fiber cashback savings
- Still need exact Wildfire/Fiber cashback rates per merchant

### Key Blockers
- Fiber staging API returns empty/times out — mock catalog fallback in use
- ERC-8004 on-chain metadata shows empty services
- Wildfire cashback rate lookup returned 404

### Tech Decisions
- MCP SDK v1.x (stable), not v2 pre-alpha
- Dual protocol: REST + MCP
- No auth for MCP (zero friction)
- Vercel serverless with in-memory store (no shared state across functions)
